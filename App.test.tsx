import React from 'react';
import { render, fireEvent } from '@testing-library/react-native';


test('App Jest Test Only Configuration Done', () => {
  const element = ('capital');
  expect(element).toBe('capital');
  });